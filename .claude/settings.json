{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; if echo \"$f\" | grep -qE '\\.rs$'; then cargo fmt 2>/dev/null || true; fi; }",
            "statusMessage": "Running cargo fmt..."
          }
        ]
      }
    ]
  }
}
